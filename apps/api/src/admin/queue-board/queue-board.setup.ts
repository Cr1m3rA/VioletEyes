import type { INestApplication } from '@nestjs/common';

/**
 * Bull-Board 挂载（Phase 6 实施）。
 * 现在只占位，避免 main.ts 引用缺失。
 */
export async function setupBullBoard(_app: INestApplication): Promise<void> {
  // TODO Phase 6: bull-board 挂载 + Basic Auth + admin guard
}